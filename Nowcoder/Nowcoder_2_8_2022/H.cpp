#include<bits/stdc++.h>



using ll = long long;
const ll LEN=1e6+7;
const ll MOD=1e8;
const int INF=INT_MAX;
const ll LLINF=LONG_LONG_MAX;
const double eps=1e-8;
const double genhao3=1.7320508075;

// int k[LEN];

int main()
{
    // freopen("in","r",stdin);
    // freopen("out","w",stdout);

    std::cin.tie(0);
    std::cout.tie(0);
    std::ios_base::sync_with_stdio(0);

    double x;
    std::cin>>x;
    // std::cout<<(double(8)/9*genhao3*x*x*x);
    printf("%llf",(double(8)/double(9)*genhao3*x*x*x));





    return 0;
}
